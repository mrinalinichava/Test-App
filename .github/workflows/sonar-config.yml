name: Build
on:
  # Trigger analysis when pushing in master or pull requests, and when creating
  # a pull request.
  pull_request:
    branches: [main]
    types:
      - opened
jobs:
  sonarQubeTrigger:
    name: SonarQube Trigger
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: SonarQube Scan
      uses: kitabisa/sonarqube-action@v1.1.0
      with:
        host: "http://ec2-44-192-64-168.compute-1.amazonaws.com:9000"
        login: "deeabed5497c362a58cc610f23144c193be4ee7c"
        projectBaseDir: ./test-app1
#         projectKey: "my-custom-project"
#         projectName: "my-custom-project-name"
#         projectVersion: "v0.0.1"
# jobs:
#   build:
#     name: Build
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#         with:
#           # Shallow clones should be disabled for a better relevancy of analysis
#           fetch-depth: 0
#       - name: Set up JDK 11
#         uses: actions/setup-java@v1
#         with:
#           java-version: 11
#       - name: Build and analyze
#         run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -DskipTests -s settings.xml
#         env:
#           # Needed to get some information about the pull request, if any
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           # SonarCloud access token should be generated from https://sonarcloud.io/account/security/
#           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
